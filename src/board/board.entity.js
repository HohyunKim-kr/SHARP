class Board {
  userId;
  category;
  id;
  title;
  writer;
  content;
  created_at;
  hit;
  constructor(userId, category, id, title, writer, content, created_at, hit) {
    this.userId = userId;
  }
}
